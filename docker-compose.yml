version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AGY_API_KEY=sk-dummy
    # Mount local gemini config if agy is running inside docker
    volumes:
      - ~/.gemini:/root/.gemini
